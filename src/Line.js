class Line {
  constructor(x, y) {
    this.x = x;
    this.y = y;
  }

  render(s) {
    // s.push();
    // s.fill(this.color);
    // s.circle(this.x, this.y, 20);
    // s.pop();
  }
}

export default Line;
